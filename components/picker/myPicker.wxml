<view>
<picker-view indicator-style="height: 50px;" style="width: 100%; height: 300px;" value="{{value}}" 

bindchange="bindChange">
    <picker-view-column>
      <view wx:for="{{city}}" wx:key="index" style="line-height: 50px; text-align: center;">{{item.areaName}}</view>
      </picker-view-column>
     

      <picker-view-column>
     <view wx:for="{{city1}}" wx:key="index" style="line-height: 50px; text-align: center;">{{item.areaName}}</view>
      </picker-view-column>

      <picker-view-column>
  <view wx:for="{{city2}}" wx:key="index" style="line-height: 50px; text-align: center;">{{item.areaName}}</view>
      </picker-view-column>
   
</picker-view>


</view>
